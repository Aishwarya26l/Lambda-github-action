name: A workflow for lambda-github-action
on: push
jobs:
  build:
    name: Test action
    runs-on: ubuntu-latest  
    steps:
    - uses: actions/checkout@master
    - uses: ./
      env:
        MY_NAME: "Aishwarya"